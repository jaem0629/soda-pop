# AGENTS.md

> 이 파일은 저장소 전체에 적용되는 공통 AI 작업 규칙을 정의한다.
> 프로젝트 디렉토리의 `AGENTS.md`는 이 문서를 전제로 하고, 프로젝트 전용 규칙만 추가한다.

## 에이전트 운영 규칙

- 코드를 작성하기 전에, 대상 프로젝트 디렉토리의 `AGENTS.md`를 반드시 읽는다.
- 하위 `AGENTS.md`는 공통 규칙을 다시 복사하지 않고, 해당 프로젝트에만 필요한 예외·제약·패턴만 추가한다.
- 공통 규칙에 예외가 필요하면 이유와 적용 범위를 해당 프로젝트 `AGENTS.md`에 함께 적는다.
- 기존 규칙으로 설명되지 않는 새로운 패턴을 도입하면, 관련 `AGENTS.md`를 함께 업데이트한다.
- 아래 변경에는 프로젝트의 `AGENTS.md`를 반드시 함께 수정한다.
  - 라이브러리/의존성을 추가하거나 제거하는 경우
  - 데이터 페칭 또는 상태 관리 패턴을 변경하는 경우
  - 새로운 공유 컴포넌트, 훅, 유틸리티를 만드는 경우
- 규칙은 `~한다`체로, 따라야 할 방법을 명확하게 적는다.
- 규칙을 수정할 때는 `기존에는 ~`와 같은 변경 이력 또는 `X 안에 두지 않고, Y에 둔다`와 같이 이전 규칙을 부정하는 형태로 작성하지 않고, 새 규칙만 단정문으로 쓴다.
- 규칙은 의사결정 분기, 비자명한 데이터 흐름, 합성 패턴, 안티패턴처럼 코드만 보고는 결정할 수 없는 것을 적는다. 이름만 봐도 자명한 사용처나 컴포넌트 카탈로그는 적지 않는다.

---

## 공통 코딩 컨벤션

모든 프로젝트에 적용된다. 프로젝트 `AGENTS.md`는 이 문서를 전제로 프로젝트 전용 규칙만 추가한다.

- 상수는 SCREAMING_SNAKE_CASE로 작성한다.
- 매직 넘버/스트링은 상수로 정의한다.
- 외부 라이브러리 설치 전에 기존 의존성으로 해결 가능한지 확인한다.
- `catch` 블록에서 에러를 처리하거나 상위로 전파한다. API 응답은 에러 상태를 확인한다.
- 커밋 메시지: `type: 설명` 형식, 한국어로 작성.
  - type: `feat`, `fix`, `refactor`, `chore`, `docs`, `test`, `style`
  - 예시: `feat: PandasAI 기반 AI 데이터 분석 챗봇 패널 추가`

---

## Frontend 공통 컨벤션

React / TypeScript 프론트엔드 프로젝트에 적용된다.

### 핵심 원칙

대부분의 문제는 **컴포넌트를 분리**함으로써 해결한다.

- 코드가 길어지면 → 컴포넌트로 분리한다.
- 스타일이 반복되면 → 컴포넌트로 추출한다.
- 로직이 복잡하면 → 컴포넌트를 나눠 각자 상태를 소유하게 한다.
- 로직과 타입은 사용하는 컴포넌트 파일에 함께 둔다. 별도 로직/타입 파일(`.ts`)을 만들지 않는다. 공유가 필요하면 도메인에 더 가까운 컴포넌트에 두고 export한다. 우열이 없으면 부모 컴포넌트에 둔다.
- 파일을 분리할 때 하위 폴더를 만들지 않는다. 같은 폴더에 두고 파일명 접두사로 그룹핑한다.

### 타입

- 타입은 `any` 대신 적절한 타입으로 구체적이게 정의한다
- 서버 데이터 타입은 자동 생성 도구(OpenAPI Generator, GraphQL Code Generator 등)가 만든 것을 사용한다. 서버 응답 타입을 수동으로 정의하지 않는다.
- 클라이언트 전용 타입(Props, UI 상태 등)은 사용하는 컴포넌트에 선언한다.
- 타입만 모은 파일은 만들지 않는다.
  - 단, 서드파티 타입 지원을 위한 `.d.ts`는 허용한다.

### 코드 구조

- 파일명은 kebab-case로, 컴포넌트명은 PascalCase로 작성한다.
- boolean 변수/props는 `is`, `has`, `should` 접두사를 사용한다.
- 한 파일에 하나의 책임만 둔다. 400줄 초과 시 컴포넌트 분리를 고려하되, 단일 책임이면 초과를 허용한다.
- HTTP 호출은 native `fetch`를 사용한다.

### 컴포넌트

- Props는 `interface XxxProps`로 정의한다.
- 페이지 컴포넌트는 `export default function XxxPage`로 작성한다.
- 공유 컴포넌트는 `export function Xxx` (named export)로 작성한다.
- 공유 컴포넌트는 `className` prop을 받아 `cn()`으로 병합한다.
- 이벤트 핸들러 구현은 `handleXxx`, props로 받는 콜백은 `onXxx`로 네이밍한다.
- 컴포넌트 내부를 유연하게 하려면 `children`으로 합성한다. `icon`, `header` 등 named slot props도 허용한다.
- 소량의 스타일 중복(2~3곳)은 추상화하지 않고 유지한다.
- 같은 스타일 조합이 반복되면 상수화 대신 컴포넌트 분리, `className` prop 합성, 기존 공유 컴포넌트를 사용한다.

### 서드파티 생성 코드

- shadcn/ui, 코드 생성기 등이 만든 코드는 이 컨벤션의 예외로 둔다. 해당 라이브러리의 관행을 따른다.
- `components/ui/` 등 서드파티 생성 디렉토리의 파일은 직접 수정하지 않는다. 필요한 확장이나 조합은 앱 레벨 래퍼 컴포넌트로 해결한다.
- 필요한 UI 프리미티브가 shadcn/ui에 있으면 직접 만들지 않고 내려받아 사용한다.

### 스타일링

- 스타일은 Tailwind CSS + `cn()` 유틸리티를 사용한다.
- 동적 값(드래그 리사이즈 등)만 예외로 inline style을 허용한다.
- 스페이싱은 4px 배수(정수값: `1`, `2`, `3`, `4`, `6`, `8` 등)를 사용한다. 소수점 단위(`px-0.5`, `py-1.5` 등)는 사용하지 않는다.
- Tailwind 임의값(bracket notation, 예: `text-[10px]`, `w-[327px]`)은 사용하지 않는다. Tailwind 기본 스케일(`text-xs`, `w-160` 등)을 사용한다. 기본 스케일로 표현 불가능한 값은 CSS 변수 또는 디자인 토큰으로 정의한다.
- 색상은 시맨틱 토큰을 사용한다: `text-foreground`, `bg-background`, `text-muted-foreground` 등. 단, 도메인 구분용 강조색(아이콘, 배지 등)은 하드코딩을 허용한다.

### 상태 관리

- state는 그것을 사용하는 가장 가까운 컴포넌트가 소유한다.
- 상태는 `useState`로 관리하고, 복잡해지면 `useReducer`를 사용한다.
- props drilling이 발생하면 컴포넌트 분리를 먼저 검토하고, 필요 시 Context를 사용한다.
- 비즈니스용 Context는 만들지 않고, 전역 Context는 theme 등 앱 전체 관심사에만 사용한다.

### 훅

- 커스텀 훅은 범용 유틸리티에만 사용한다 (`useDebounce`, `useUrlState` 등).
- 비즈니스 로직이 공통되면 컴포넌트를 분리하고 합성으로 해결한다.
- 훅은 `export function useXxx`, 파일명 `use-xxx.ts`로 작성한다.

### React API

- 파생 값은 렌더링 중에 직접 계산한다. state + Effect로 동기화하지 않는다.
- 상태 초기화가 필요하면 `key` prop을 사용한다.
- `useEffect`는 외부 시스템 동기화에만 사용한다 (WebSocket, 타이머, DOM 리스너 등). 사용자 이벤트는 이벤트 핸들러에서 처리한다.
- `useEffectEvent`는 Effect 내부에서 최신 값을 읽되 재실행이 불필요할 때 사용한다. Effect 내부에서만 호출하고, 의존성 배열에 포함하지 않는다.
- `useLayoutEffect`는 브라우저 repaint 전 레이아웃 측정이 필요한 경우에만 사용한다.
- React Compiler를 사용한다. 수동 `useMemo`, `useCallback`, `React.memo`를 작성하지 않는다.
